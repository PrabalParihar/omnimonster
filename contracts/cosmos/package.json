{
  "name": "@swap-sage/contracts-cosmos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "clean": "cargo clean",
    "schema": "cargo run --bin schema"
  }
}